---

- hosts: all
  become: yes
  tasks:
    - name: Set authorized key took from file
      authorized_key:
        user: root
        state: present
        key: "{{ lookup('file', '/home/ulinux/.ssh/id_rsa.pub') }}"